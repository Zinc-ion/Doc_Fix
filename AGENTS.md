# Doc_Fix — 项目协作规则

## 1. 项目定位

Doc_Fix 是一个面向 WPS/Word 申报书、标书文档的格式检查工具。v1 优先处理实际提交中常见的 `.doc` 文件：先将 `.doc` 转换并保留为 `.docx` 工作副本，再基于工作副本进行规则抽取、检查和报告。

**v1 核心目标**：优先检查刚性限制，尤其是各板块字数限制、表格格式、图片格式，减少人工审核中最容易出错且最影响提交结果的部分。

## 2. v1 范围与优先级

### P0 刚性限制

- **板块字数限制**：从模板中自动识别“限xxxx字以内”“不超过xxxx字”“每个课题限xxxx字以内”等规则。
- **表格格式检查**：检查表格数量、列数、宽度、边框、对齐、字体、行高和是否超出页面宽度。
- **图片格式检查**：检查图片数量、所在板块、宽高、段落对齐、环绕方式和是否超出页面宽度。

### P1 弱刚性限制

- 页面设置、页边距、字体、字号、行距、标题样式、编号、页眉页脚等可逐步扩展。
- v1 可以记录这些信息，但不得牺牲 P0 刚性检查的稳定性。

### 暂不承诺

- v1 不直接修改原始 `.doc` 文件。
- v1 不承诺自动修正。
- v1 不承诺 `.docx` 回转 `.doc` 绝对保真。
- Word 图表、Excel 嵌入对象等复杂 OLE 对象不作为 v1 强制范围。

## 3. 技术栈决策

| 用途 | 技术 |
|------|------|
| 语言 | Python 3.10+ |
| `.doc` 转换 | Windows 本机 Microsoft Word / Word Converter |
| `.docx` 文档读写 | `python-docx` |
| 底层 OOXML 访问 | `lxml` |
| CLI 框架 | `click` |
| 终端输出 | `rich` |
| AI 辅助 | DeepSeek OpenAI-compatible API + `openai` + `python-dotenv` |
| 测试框架 | `pytest` |
| 包管理 | `pip` + `requirements.txt` |

### 技术约束

- `.doc` 是老二进制格式，`python-docx` 不能直接读取，必须先通过转换适配层生成 `.docx` 工作副本。
- 原始 `.doc` 文件只读处理，不得覆盖或静默修改。
- 转换后的 `.docx` 副本必须保留，便于人工复核和后续问题定位。
- 检查模块只消费标准化后的 `.docx`，不得在业务检查逻辑中混入 Word COM 调用。
- `.docx -> .doc` 只允许作为后续导出能力，且导出后必须再次检查刚性规则。
- DeepSeek 仅作为可选辅助摘要和人工建议来源，不得改变刚性规则的 pass/fail 结论。
- API key 必须通过本地 `.env` 或环境变量提供，禁止写入代码、文档示例、测试输出或报告。
- v1 不引入 Web 框架。

## 4. 架构规则

### 模块边界

```
model/          <- 数据模型，仅依赖 Python 标准库
converter/      <- 输入标准化，负责 .doc <-> .docx 转换适配
extractor/      <- 依赖 model，读取 .docx 产生模板规则和文档结构
checker/        <- 依赖 model，比对目标文档与刚性规则
reporter/       <- 依赖 model，输出终端报告和 JSON 报告
cli/            <- 依赖各模块，负责编排流程
corrector/      <- 后续阶段模块，v1 不作为必做范围
```

### 依赖方向

```
cli -> converter/extractor/checker/reporter -> model
```

禁止反向依赖，禁止横向耦合。`extractor/` 不依赖 `checker/`，`checker/` 不直接读取文件。

### 关键原则

1. **转换与检查分离**：`.doc` 转换失败应在 `converter/` 层给出明确错误，检查逻辑不处理二进制 `.doc`。
2. **刚性规则优先**：字数、表格、图片检查是 v1 主线，其他格式项不得扩大实现范围。
3. **模板自动抽取为主**：优先从模板正文提示语抽取规则，JSON 配置文件只用于覆盖异常、补充缺失规则或禁用误识别规则。
4. **报告可追溯**：每条差异必须能定位到板块、规则来源、期望值、实际值和原始文件/转换副本路径。
5. **报告可定位**：面向人工核对的 Markdown/HTML 报告必须按目标文档章节路径分组，尽量提供附近标题、表题/图题、章节内序号和内容摘录。
6. **不静默通过不确定项**：无法稳定识别板块边界或图表对象时，应报告“需人工确认”。

## 5. 字数规则

- 默认识别模板中的“限1500字以内”“限2000字以内（不包括表格）”“每个课题限3000字以内”等提示。
- JSON 配置可覆盖、追加或禁用模板自动识别的字数规则。
- 中文按字符计数。
- 英文、数字、连续字母数字串按词计数。
- 标点、空白、模板提示文字不计入正文。
- 规则包含“不包括表格”时，该板块内表格文本不计入字数。
- 板块范围从规则锚点之后开始，到下一个同级或更高层级标题/规则锚点之前结束。

## 6. `.docx` 回转 `.doc` 规则

- 技术上可通过 Microsoft Word 将 `.docx` 另存为 `.doc`。
- 不得在文档或 CLI 文案中承诺 `.docx -> .doc` 绝对无损。
- 若后续实现自动修正，应输出 `.docx` 修正版和 `.doc` 导出版，并对导出的 `.doc` 再转换、再检查。
- 最终报告必须明确导出的 `.doc` 是否通过二次刚性规则验证。

## 7. 代码规范

- 遵循 PEP 8，使用 4 空格缩进。
- 所有公开函数/方法必须有类型标注。
- 数据类使用 `@dataclass(frozen=True)`，除非确有可变状态需求。
- 模块/文件使用 `snake_case`，类使用 `PascalCase`，函数/方法使用 `snake_case`，常量使用 `UPPER_SNAKE`。
- 公开 API 通过 `__init__.py` 导出。
- 代码注释只解释不明显的 WHY，不重复描述 WHAT。

## 8. 测试要求

- 使用 `pytest`。
- `example/` 下的真实 `.doc` 模板可作为早期人工验证样本；自动化测试 fixture 应放在 `tests/fixtures/`。
- 必须覆盖 `.doc -> .docx` 转换成功、转换失败、密码保护、文件损坏等场景。
- 必须覆盖字数规则抽取、混排字数统计、表格文本排除、板块边界无法确认等场景。
- 必须覆盖表格宽度/边框/列数/字体/对齐和图片尺寸/位置/超页宽检查。

## 9. 开发工作流

- `main` 为稳定分支，只通过 PR 合并。
- 功能分支命名：`feat/<功能简述>`。
- 修复分支命名：`fix/<问题简述>`。
- 提交信息格式：`<type>: <简短描述>`，如 `feat: add doc converter`。
- PR 描述需说明变更动机、影响范围和测试结果。

## 10. 文档维护

- `AGENTS.md`：项目协作规则，所有贡献者必读。
- `progress.md`：项目构思、可行性、当前进度、风险和里程碑。
- `README.md`：项目简介和快速开始。
- `.env.example`：本地环境变量模板，不包含真实密钥。
- 每次改变范围、依赖、风险判断或里程碑时，必须同步更新 `progress.md`。

## 11. 禁止事项

- 禁止私自引入新依赖，需先在 `progress.md` 中记录原因和影响。
- 禁止在 `model/` 层引入外部依赖。
- 禁止跨模块横向导入。
- 禁止覆盖用户原始 `.doc` 文件。
- 禁止承诺 `.docx` 回转 `.doc` 绝对保真。
- 禁止提交包含密钥、密码、内网地址的代码。
- 禁止提交 `.env` 或任何包含真实 API key 的文件。
