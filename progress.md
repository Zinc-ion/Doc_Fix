# Doc_Fix — 项目构思与进度

## 项目愿景

**一句话**：让 WPS/Word `.doc` 申报书的刚性格式检查从人工核对变成一条命令自动完成。

实际申报材料中，用户大概率需要提交 `.doc` 文件，而不是 `.docx` 文件。Doc_Fix v1 因此不再追求完整 Word 格式检查，而是优先解决最刚性的两类问题：各板块字数限制，以及表格/图片格式限制。其他页面、字体、样式等格式项先作为弱刚性能力逐步扩展。

## 可行性结论

项目可行，但必须采用转换链路。

- `.doc` 是老二进制 Word 格式，`python-docx` 不能直接读取。
- v1 需要先调用 Windows 本机 Microsoft Word / Word Converter，将 `.doc` 转为 `.docx` 工作副本。
- 转换后的 `.docx` 可使用 `python-docx + lxml` 读取段落、表格、图片和底层 OOXML。
- 当前环境已发现 Microsoft Word / Word Converter，适合作为 v1 首选转换后端。
- `example/` 下两个真实模板均为 `.doc`，且能识别出大量“限xxxx字以内”“限xxxx字以内（不包括表格）”提示，说明模板自动抽取字数规则具备实现价值。

主要限制是：`.doc <-> .docx` 转换不保证绝对保真，尤其是复杂表格、图片锚点、编号、页眉页脚、WPS 兼容属性和嵌入对象。v1 只检查，不修改原始 `.doc`。

## v1 成功标准

- 支持输入 `.doc` 模板和 `.doc` 目标文件。
- 自动生成并保留 `.docx` 工作副本。
- 从模板自动抽取“限xxxx字以内”“不超过xxxx字”“每个课题限xxxx字以内”等字数规则。
- 按“中文字符 + 英文/数字词”统计正文。
- 支持“不包括表格”的字数规则。
- 检查表格的数量、列数、宽度、边框、对齐、字体、行高和是否超页宽。
- 检查图片的数量、所在板块、宽高、段落对齐、环绕方式和是否超页宽。
- 输出终端报告和 JSON 报告，报告中包含板块、规则来源、期望值、实际值、文件路径和需人工确认项。

## 核心功能优先级

| 优先级 | 功能 | 描述 |
|--------|------|------|
| P0 | `.doc` 输入标准化 | 将模板和目标 `.doc` 转换为 `.docx` 副本并保留 |
| P0 | 字数规则抽取 | 从模板提示语自动抽取板块字数限制 |
| P0 | 字数检查 | 按中文字符 + 英文/数字词统计目标板块内容 |
| P0 | 表格格式检查 | 检查表格结构、边框、宽度、字体、对齐和超页宽 |
| P0 | 图片格式检查 | 检查图片尺寸、位置、对齐、环绕和超页宽 |
| P0 | 差异报告 | 输出终端摘要和 JSON 详情 |
| P1 | 配置覆盖 | 使用 JSON/YAML 覆盖或补充模板自动抽取规则 |
| P1 | 弱刚性格式检查 | 页面、字体、标题样式、行距、编号、页眉页脚 |
| P2 | 自动修正 | 输出 `.docx` 修正版，并评估导出 `.doc` |
| P2 | `.doc` 导出验证 | `.docx -> .doc` 后再次检查刚性规则 |
| P3 | 批量处理 | 一次检查多个申报书 |

## 当前阶段：Phase 0 — 文档与方向校准

**状态**：进行中

**已完成**：

- [x] 仓库创建
- [x] 初版 README
- [x] 明确 `.doc` 是 v1 优先输入格式
- [x] 明确 v1 优先检查字数、表格和图片格式
- [x] 验证 `example/` 下真实模板存在可抽取的字数限制提示
- [x] 记录 `.docx -> .doc` 不能承诺绝对保真的风险
- [x] 重写 `AGENTS.md` 项目协作规则
- [x] 重写 `progress.md` 项目构思与进度

**待完成**：

- [ ] 搭建 Python 包目录结构
- [ ] 创建 `requirements.txt`
- [ ] 实现 `converter/` 原型
- [ ] 实现字数规则抽取原型
- [ ] 实现表格/图片格式提取原型

**下一目标**：Phase 1 — `.doc` 转换适配层与真实样本验证

## 里程碑路线图

### Phase 1：`.doc` 转换适配层

- **目标**：稳定接收 `.doc/.docx` 输入，并统一产出 `.docx` 工作副本。
- **交付物**：`converter/` 模块、转换错误模型、基础测试。
- **重点**：Word/Word Converter 探测、只读打开、输出目录管理、转换失败提示、保留副本路径。

### Phase 2：字数规则抽取与统计

- **目标**：从模板中抽取板块字数规则，并检查目标文档是否超限。
- **交付物**：规则模型、抽取器、字数统计器、字数检查报告。
- **重点**：“限xxxx字以内”“不超过xxxx字”“每个课题限xxxx字以内”“不包括表格”等模式。

### Phase 3：表格与图片格式检查

- **目标**：检查模板和目标文档中的表格/图片关键格式差异。
- **交付物**：表格格式模型、图片格式模型、格式检查器。
- **重点**：表格边框、列数、宽度、字体、对齐、行高、图片尺寸、图片对齐、超页宽。

### Phase 4：CLI 与报告

- **目标**：提供可用命令行和稳定报告格式。
- **交付物**：`doc-fix check` 命令、终端报告、JSON 报告。
- **规划命令**：

```bash
doc-fix check --template template.doc --input target.doc --out-dir output/
doc-fix check --template template.doc --input target.doc --json output/report.json
```

### Phase 5：测试与硬化

- **目标**：处理真实文档中的边界情况，提高结果可信度。
- **交付物**：测试 fixtures、集成测试、错误用例。
- **重点**：密码保护、损坏文件、转换失败、复杂表格、嵌套表格、图片锚点、无法确认板块边界。

### Phase 6：自动修正与 `.doc` 导出评估

- **目标**：评估自动修正和提交 `.doc` 文件的可行性。
- **交付物**：`.docx` 修正版、`.doc` 导出版、导出后二次检查报告。
- **原则**：不承诺无损，只交付可验证结果。

## 字数规则设计

- 默认从模板正文自动抽取规则。
- 配置文件用于覆盖自动抽取错误、补充模板缺失规则或禁用某条规则。
- 报告中必须标明规则来源：`template` 或 `config`。
- 中文字符按 1 计数。
- 英文、数字、连续字母数字串按 1 个词计数。
- 标点、空白、模板提示语不计数。
- 出现“不包括表格”时，该板块内表格文本不计入字数。
- 无法稳定识别板块边界时，报告为“需人工确认”，不得静默通过。

## 表格与图片检查设计

表格 v1 优先检查：

- 表格数量和所在板块。
- 表格标题或附近锚点。
- 列数、整体宽度、是否超页宽。
- 边框是否存在、线型、线宽。
- 单元格文字字体、字号、对齐方式。
- 行高和关键单元格合并情况。

图片 v1 优先检查：

- 图片数量和所在板块。
- 图片宽高和是否超页宽。
- 段落对齐方式。
- 环绕方式。
- 图片附近标题或说明文字。

Word 图表、Excel 嵌入对象、复杂 OLE 对象暂不作为 v1 强制检查范围，遇到时记录为需人工确认。

## `.docx` 回转 `.doc` 结论

- 技术上可以通过 Microsoft Word 将 `.docx` 另存为 `.doc`。
- 不能保证 100% 保持相同格式。
- `.doc` 是老二进制格式，复杂 OOXML 样式、图片锚点、表格布局、编号、页眉页脚和 WPS 兼容属性可能发生变化。
- v1 只检查，不修改原 `.doc`。
- 后续若实现自动修正，应输出 `.docx` 修正版和 `.doc` 导出版。
- 导出 `.doc` 后必须再次转换/检查，确认刚性规则仍通过。
- 最终报告要明确 `.doc` 导出版是否通过二次验证。

## 当前问题与风险

| 问题 | 影响 | 当前策略 |
|------|------|----------|
| `.doc` 不能直接读取 | 必须依赖转换器 | 使用本机 Word/Word Converter 转 `.docx` |
| 转换保真度不绝对 | 可能影响表格/图片布局判断 | 保留副本，报告转换路径和需人工确认项 |
| WPS 与 Word 排版差异 | Word 转换结果可能和 WPS 视觉不同 | v1 优先检查刚性规则，不承诺像素级一致 |
| 板块边界不总是清晰 | 字数统计可能误包含或漏包含内容 | 无法判断时报告需人工确认 |
| 表格内文字是否计数 | 不同规则要求不同 | 支持“不包括表格”并在报告中说明 |
| 图片和嵌入对象类型复杂 | OLE/图表对象难以稳定检查 | v1 只强制图片和普通表格，其他记录风险 |
| 自动修正后提交 `.doc` | 回转 `.doc` 可能改变格式 | 放入后续阶段，并要求二次检查 |

## 变更日志

| 日期 | 变更 | 原因 |
|------|------|------|
| 2026-05-16 | 项目初始化，创建 README、Agents.md、progress.md | 项目启动 |
| 2026-05-16 | 调整为 `.doc` 优先，v1 聚焦字数、表格、图片刚性检查 | 申报书实际大概率以 `.doc` 提交 |
| 2026-05-16 | 将 `Agents.md` 规范化为 `AGENTS.md` | 兼容常见 agent 协作文件命名 |
