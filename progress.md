# Doc_Fix — 项目构思与进度

## 项目愿景

**一句话**：让 WPS/Word `.doc` 申报书的刚性格式检查从人工核对变成一条命令自动完成。

实际申报材料中，用户大概率需要提交 `.doc` 文件，而不是 `.docx` 文件。Doc_Fix v1 因此不再追求完整 Word 格式检查，而是优先解决最刚性的两类问题：各板块字数限制，以及表格/图片格式限制。其他页面、字体、样式等格式项先作为弱刚性能力逐步扩展。

## 可行性结论

项目可行，但必须采用转换链路。

- `.doc` 是老二进制 Word 格式，`python-docx` 不能直接读取。
- v1 需要先调用 Windows 本机 Microsoft Word / Word Converter，将 `.doc` 转为 `.docx` 工作副本。
- 转换后的 `.docx` 可使用 `python-docx + lxml` 读取段落、表格、图片和底层 OOXML。
- 当前环境已发现 Microsoft Word / Word Converter，适合作为 v1 首选转换后端。
- `example/` 下两个真实模板均为 `.doc`，且能识别出大量“限xxxx字以内”“限xxxx字以内（不包括表格）”提示，说明模板自动抽取字数规则具备实现价值。

主要限制是：`.doc <-> .docx` 转换不保证绝对保真，尤其是复杂表格、图片锚点、编号、页眉页脚、WPS 兼容属性和嵌入对象。v1 只检查，不修改原始 `.doc`。

## v1 成功标准

- 支持输入 `.doc` 模板和 `.doc` 目标文件。
- 自动生成并保留 `.docx` 工作副本。
- 从模板自动抽取“限xxxx字以内”“不超过xxxx字”“每个课题限xxxx字以内”等字数规则。
- 按“中文字符 + 英文/数字词”统计正文。
- 支持“不包括表格”的字数规则。
- 检查表格的数量、列数、宽度、边框、对齐、字体、行高和是否超页宽。
- 检查图片的数量、所在板块、宽高、段落对齐、环绕方式和是否超页宽。
- 输出终端报告和 JSON 报告，报告中包含板块、规则来源、期望值、实际值、文件路径和需人工确认项。
- 支持 Markdown/HTML 人工核对报告。
- Markdown/HTML 报告按目标文档目录章节分组，定位到章节路径、表题/图题、章节内序号和内容摘录。
- 默认将 JSON/Markdown/HTML 报告统一输出到 `--out-dir`，减少命令行参数。
- 可选启用 DeepSeek 生成辅助摘要、处理建议和收尾复核项，但不改变刚性规则结论。

## 核心功能优先级

| 优先级 | 功能 | 描述 |
|--------|------|------|
| P0 | `.doc` 输入标准化 | 将模板和目标 `.doc` 转换为 `.docx` 副本并保留 |
| P0 | 字数规则抽取 | 从模板提示语自动抽取板块字数限制 |
| P0 | 字数检查 | 按中文字符 + 英文/数字词统计目标板块内容 |
| P0 | 表格格式检查 | 检查表格结构、边框、宽度、字体、对齐和超页宽 |
| P0 | 图片格式检查 | 检查图片尺寸、位置、对齐、环绕和超页宽 |
| P0 | 差异报告 | 输出终端摘要和 JSON 详情 |
| P1 | 人工报告 | 输出 Markdown/HTML 检查报告 |
| P1 | AI 辅助 | DeepSeek 生成报告摘要、人工核对建议和结构化人工复核项 |
| P1 | 配置覆盖 | 使用 JSON 覆盖、补充或禁用模板自动抽取规则 |
| P1 | 弱刚性格式检查 | 页面、字体、标题样式、行距、编号、页眉页脚 |
| P2 | 自动修正 | 输出 `.docx` 修正版，删除备注并对齐段落/表格文字格式 |
| P2 | `.doc` 导出验证 | `.docx -> .doc` 后再次检查刚性规则 |
| P3 | 批量处理 | 一次检查多个申报书 |

## 当前阶段：Phase 1 — `.doc` 转换适配层与 v1 原型

**状态**：进行中

**已完成**：

- [x] 仓库创建
- [x] 初版 README
- [x] 明确 `.doc` 是 v1 优先输入格式
- [x] 明确 v1 优先检查字数、表格和图片格式
- [x] 验证 `example/` 下真实模板存在可抽取的字数限制提示
- [x] 记录 `.docx -> .doc` 不能承诺绝对保真的风险
- [x] 重写 `AGENTS.md` 项目协作规则
- [x] 重写 `progress.md` 项目构思与进度
- [x] 创建 Python 包目录结构
- [x] 创建 `requirements.txt` 和 `pyproject.toml`
- [x] 实现 `converter/` 原型，支持 `.doc/.docx` 标准化为 `.docx` 工作副本
- [x] 实现模板字数规则抽取原型
- [x] 实现字数、表格、图片检查原型
- [x] 实现 `doc-fix check` CLI、Rich 终端报告和 JSON 报告
- [x] 添加聚焦单元测试
- [x] 提高板块边界识别：从提示语后开始统计，并支持按标题层级截断
- [x] 支持 JSON 配置覆盖、追加和禁用字数规则
- [x] 补充表格检查项：行数、样式、对齐、边框签名、列宽、行高、字体、字号、单元格对齐
- [x] 补充图片检查项：宽高、环绕方式、段落对齐和正文宽度超限
- [x] 新增 Markdown/HTML 人工核对报告
- [x] 重构报告定位能力：按目标文档章节路径分组，并为字数/表格/图片问题附加 locator、caption 和 content preview
- [x] 字数规则支持按模板章节路径兜底定位：目标文档缺少模板字数提示语时仍按对应章节检查
- [x] 新增 DeepSeek 可选 AI 辅助摘要与建议
- [x] 新增 DeepSeek 收尾复核：输出不影响刚性结论的结构化人工确认项
- [x] 使用 `.env` 管理 DeepSeek API key，并提交 `.env.example`
- [x] 新增 P1 字体字号与段落格式检查：默认按模板对比正文/标题段落，支持 JSON 配置覆盖检查项和容差，默认仅输出 warning
- [x] 新增目标文档标注审核副本：默认生成 `input.annotated.docx`，对可定位 issue 添加黄色高亮和 Word 批注
- [x] 新增 `doc-fix correct` 自动修正原型：输出 `input.corrected.docx`，删除 `【】` 备注并按模板对齐正文/标题段落和表格文字格式
- [x] 自动修正后尝试导出 `input.corrected.doc`，并生成修正版 `.docx` 与导出 `.doc` 的二次检查 JSON 报告

**待完成**：

- [ ] 为真实 `.doc` 样本补充更细的集成测试 fixture
- [ ] 进一步识别正文/模板说明文字边界，降低示例文字被误计的概率
- [ ] 将 JSON 配置扩展到表格/图片容差和检查项开关
- [ ] 增加表格文本是否计入字数的精确位置判断
- [ ] 控制 AI 输入内容范围，支持用户选择是否发送更多上下文
- [ ] 使用真实申报书样本校准段落格式检查的误报率，必要时细化章节/样式匹配策略
- [ ] 使用真实 `.doc` 样本验证标注位置与 Word/WPS 审阅视图兼容性
- [ ] 使用真实申报书样本验证自动修正版在 Word/WPS 中的视觉一致性，并校准格式兜底匹配策略

**下一目标**：Phase 2 — 真实样本集成测试、规则容差配置与自动修正硬化

## 里程碑路线图

### Phase 1：`.doc` 转换适配层

- **目标**：稳定接收 `.doc/.docx` 输入，并统一产出 `.docx` 工作副本。
- **交付物**：`converter/` 模块、转换错误模型、基础测试。
- **重点**：Word/Word Converter 探测、只读打开、输出目录管理、转换失败提示、保留副本路径。

### Phase 2：字数规则抽取与统计

- **目标**：从模板中抽取板块字数规则，并检查目标文档是否超限。
- **交付物**：规则模型、抽取器、字数统计器、字数检查报告。
- **重点**：“限xxxx字以内”“不超过xxxx字”“每个课题限xxxx字以内”“不包括表格”等模式。

### Phase 3：表格与图片格式检查

- **目标**：检查模板和目标文档中的表格/图片关键格式差异。
- **交付物**：表格格式模型、图片格式模型、格式检查器。
- **重点**：表格边框、列数、宽度、字体、对齐、行高、图片尺寸、图片对齐、超页宽。

### Phase 4：CLI 与报告

- **目标**：提供可用命令行和稳定报告格式。
- **交付物**：`doc-fix check` 命令、终端报告、JSON 报告。
- **规划命令**：

```bash
doc-fix check --template template.doc --input target.doc --out-dir output/
doc-fix check --template template.doc --input target.doc --out-dir output/ --ai
doc-fix check --template template.doc --input target.doc --out-dir output/ --ai-review
```

### Phase 5：测试与硬化

- **目标**：处理真实文档中的边界情况，提高结果可信度。
- **交付物**：测试 fixtures、集成测试、错误用例。
- **重点**：密码保护、损坏文件、转换失败、复杂表格、嵌套表格、图片锚点、无法确认板块边界。

### Phase 6：自动修正与 `.doc` 导出评估

- **目标**：评估自动修正和提交 `.doc` 文件的可行性。
- **交付物**：`.docx` 修正版、`.doc` 导出版、导出后二次检查报告。
- **原则**：不承诺无损，只交付可验证结果。

## 字数规则设计

- 默认从模板正文自动抽取规则。
- 配置文件用于覆盖自动抽取错误、补充模板缺失规则或禁用某条规则。
- 报告中必须标明规则来源：`template` 或 `config`。
- 模板抽取的字数规则需保留模板章节路径；目标文档缺少模板提示语时，应优先用相同章节路径定位并继续检查。
- 中文字符按 1 计数。
- 英文、数字、连续字母数字串按 1 个词计数。
- 标点、空白、模板提示语不计数。
- 出现“不包括表格”时，该板块内表格文本不计入字数。
- 无法稳定识别板块边界时，报告为“需人工确认”，不得静默通过。

## 表格与图片检查设计

表格 v1 优先检查：

- 表格数量和所在板块。
- 表格标题或附近锚点。
- 列数、整体宽度、是否超页宽。
- 边框是否存在、线型、线宽。
- 单元格文字字体、字号、对齐方式。
- 行高和关键单元格合并情况。

图片 v1 优先检查：

- 图片数量和所在板块。
- 图片宽高和是否超页宽。
- 段落对齐方式。
- 环绕方式。
- 图片附近标题或说明文字。

Word 图表、Excel 嵌入对象、复杂 OLE 对象暂不作为 v1 强制检查范围，遇到时记录为需人工确认。

## 字体字号与段落格式检查设计

- 字体字号与段落格式属于 P1 弱刚性检查，默认 issue 级别为 `warning`，不影响刚性规则 `passed` 结论和 CLI 退出码。
- 首版覆盖正文与标题段落，检查显式字体、字号、段落对齐、首行/左右缩进、行距、段前段后。
- 默认采用模板对比：优先匹配相同章节路径和段落类型，找不到时使用同类型全局代表格式兜底。
- JSON 配置通过 `format_rules` 覆盖检查项开关和容差，继续复用现有 `--config` 入口。
- 首版不解析 Word 样式继承后的最终渲染值，不检查编号、项目符号、页眉页脚和页面设置。

## 标注审核副本设计

- 检查完成后默认基于目标 `.docx` 工作副本生成 `input.annotated.docx`，原始 `.doc/.docx` 和 `input.converted.docx` 均不被修改。
- 对所有 issue 尽量在对应段落、表格首个非空单元格、图片所在段落或附近定位处添加黄色高亮和 Word 批注。
- 批注内容包含 issue code、severity、问题说明、定位信息和期望/实际值，作为 Markdown/HTML/JSON 报告之外的人工审核入口。
- 无法定位到可批注 run 时记录 `annotation_warnings`，不改变刚性 pass/fail 结论。
- 首版不将标注副本回转为 `.doc`，避免引入 `.docx -> .doc` 保真承诺。

## 自动修正设计

- 自动修正通过 `doc-fix correct` 独立入口执行，`doc-fix check` 保持纯检查行为不变。
- 原始 `.doc/.docx` 只读处理；修正只写入 `--out-dir` 下的 `input.corrected.docx` 和可选 `input.corrected.doc`。
- 首版无条件删除正文和表格单元格中所有成对 `【...】` 备注；正文段落若删除后为空则删除该段落，表格单元格内保留表格结构。
- 首版自动对齐正文、标题段落和表格单元格文字的显式字体、字号、字体颜色、字体背景/高亮、段落对齐、缩进、行距、段前段后。
- 格式匹配复用检查阶段策略：优先同章节路径和同段落类型，找不到时按同类型全局模板格式兜底；表格文字优先按同序号表格和同位置单元格匹配。
- 不自动调整图片、表格宽度、表格结构、页眉页脚、编号、页面设置和 Word 样式继承后的最终渲染值。
- 修正后生成 `correction_report.json`、`corrected_check_report.json`；若 `.doc` 导出成功，还会生成 `corrected_doc_check_report.json`。

## AI 收尾硬化设计

- AI 收尾复核只输出人工确认项，不得改变 `passed`、退出码或刚性 issue 的 error/warning 结论。
- 默认输入为结构化报告中的 issue、章节路径、locator、caption 和内容摘录，不发送 API key 或完整原始文档。
- 复核项使用 `ai_review_findings` 输出，包含 code、message、confidence、chapter_path、locator、evidence 和 suggested_action。
- 优先让 AI 判断程序难以稳定区分的边界：模板说明文字、章节边界不确定、表格文字是否计数、转换排版风险和疑似误报。
- Markdown/HTML 报告单独展示“AI 收尾复核”，作为人工核对线索，不计入刚性问题数量。

## `.docx` 回转 `.doc` 结论

- 技术上可以通过 Microsoft Word 将 `.docx` 另存为 `.doc`。
- 不能保证 100% 保持相同格式。
- `.doc` 是老二进制格式，复杂 OOXML 样式、图片锚点、表格布局、编号、页眉页脚和 WPS 兼容属性可能发生变化。
- v1 只检查，不修改原 `.doc`。
- 后续若实现自动修正，应输出 `.docx` 修正版和 `.doc` 导出版。
- 导出 `.doc` 后必须再次转换/检查，确认刚性规则仍通过。
- 最终报告要明确 `.doc` 导出版是否通过二次验证。

## 当前问题与风险

| 问题 | 影响 | 当前策略 |
|------|------|----------|
| `.doc` 不能直接读取 | 必须依赖转换器 | 使用本机 Word/Word Converter 转 `.docx` |
| 转换保真度不绝对 | 可能影响表格/图片布局判断 | 保留副本，报告转换路径和需人工确认项 |
| WPS 与 Word 排版差异 | Word 转换结果可能和 WPS 视觉不同 | v1 优先检查刚性规则，不承诺像素级一致 |
| 板块边界不总是清晰 | 字数统计可能误包含或漏包含内容 | 程序无法判断时报告需人工确认，`--ai-review` 可补充人工复核线索 |
| 表格内文字是否计数 | 不同规则要求不同 | 支持“不包括表格”并在报告中说明 |
| 图片和嵌入对象类型复杂 | OLE/图表对象难以稳定检查 | v1 只强制图片和普通表格，其他记录风险 |
| 自动修正后提交 `.doc` | 回转 `.doc` 可能改变格式 | 放入后续阶段，并要求二次检查 |

## 变更日志

| 日期 | 变更 | 原因 |
|------|------|------|
| 2026-05-16 | 项目初始化，创建 README、Agents.md、progress.md | 项目启动 |
| 2026-05-16 | 调整为 `.doc` 优先，v1 聚焦字数、表格、图片刚性检查 | 申报书实际大概率以 `.doc` 提交 |
| 2026-05-16 | 将 `Agents.md` 规范化为 `AGENTS.md` | 兼容常见 agent 协作文件命名 |
| 2026-05-16 | 实现 v1 可运行原型：转换、抽取、检查、报告、CLI 和单元测试 | 开始进入实际开发 |
| 2026-05-16 | 增强板块边界、JSON 规则覆盖、表格/图片细项检查 | 提升 v1 检查精度和可配置性 |
| 2026-05-16 | 新增 Markdown/HTML 报告和 DeepSeek 可选辅助 | 提升人工核对体验 |
| 2026-05-16 | 重构报告定位能力，按目标文档目录章节分组 | 解决问题无法在 WPS 中定位的问题 |
| 2026-05-16 | 统一报告默认输出到 `--out-dir` | 简化日常运行命令 |
| 2026-05-16 | 字数规则支持按模板章节路径兜底定位 | 目标文档通常不保留模板字数提示语，仍需按模板规则稳定检查 |
| 2026-05-17 | 新增 AI 收尾复核设计与 `--ai-review` 输出 | 将程序难以硬编码的语义边界交给模型辅助人工确认，但不改变刚性结论 |
| 2026-05-18 | 在 `AGENTS.md` 记录 `doc_fix` conda 虚拟环境和测试命令 | 避免后续协作误用 Anaconda `base` 或其他 Python 运行测试 |
| 2026-05-18 | 新增字体字号与段落格式检查设计和实现 | 将 P1 弱刚性格式项纳入报告，保持默认 warning 策略以降低首版误报成本 |
| 2026-05-18 | 新增目标文档标注审核副本设计和实现 | 让人工审核可直接在 Word/WPS 中查看黄色高亮与审阅批注 |
| 2026-05-18 | 新增 `doc-fix correct` 自动修正原型 | 项目从检测阶段进入自动修改阶段，先覆盖备注删除和段落/表格文字格式对齐 |
